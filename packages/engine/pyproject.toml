[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "regelrecht-engine"
version = "0.2.0"
requires-python = ">=3.10"

[tool.maturin]
features = ["python"]
module-name = "regelrecht_engine"
